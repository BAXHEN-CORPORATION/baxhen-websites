import React from 'react'

/**
 * Layout for Baxhen's own pages (landing page, portfolio, dashboard).
 */
export default function BaxhenLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
